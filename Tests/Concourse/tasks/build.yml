platform: linux

image_resource:
  type: registry-image
  source:
    repository: mcr.microsoft.com/dotnet/sdk
    tag: "8.0"

inputs:
  - name: repo
  - name: restored-repo

outputs:
  - name: built-repo
  - name: build-status

run:
  path: bash
  args:
    - repo/Tests/Concourse/scripts/build.sh

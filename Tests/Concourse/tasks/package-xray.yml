platform: linux

image_resource:
  type: registry-image
  source:
    repository: alpine
    tag: "3.20"

inputs:
  - name: repo
  - name: source-metadata
  - name: test-results
  - name: allure-report

outputs:
  - name: xray-report-artifact

run:
  path: sh
  args:
    - repo/Tests/Concourse/scripts/package-xray.sh

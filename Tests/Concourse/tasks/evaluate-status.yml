platform: linux

image_resource:
  type: registry-image
  source:
    repository: alpine
    tag: "3.20"

inputs:
  - name: repo
  - name: test-results

run:
  path: sh
  args:
    - repo/Tests/Concourse/scripts/evaluate-status.sh

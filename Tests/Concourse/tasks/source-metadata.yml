platform: linux

image_resource:
  type: registry-image
  source:
    repository: alpine/git
    tag: latest

inputs:
  - name: repo

outputs:
  - name: source-metadata

run:
  path: sh
  args:
    - repo/Tests/Concourse/scripts/source-metadata.sh
